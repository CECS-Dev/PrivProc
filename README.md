# PrivProc
This is the project for a desktop utility that prepares documents containing sensitive data for processing via AI models by redacting potentially private strings which it will then parse back into a final document after processing.
